{
    "configurations": [
      {
        "type": "pwa-node",
        "request": "launch",
        "name": "Launch Expo App",
        "program": "${workspaceFolder}/node_modules/expo/bin/cli.js",
        "args": ["start"],
        "cwd": "${workspaceFolder}",
        "console": "integratedTerminal"
      }
    ]
  }